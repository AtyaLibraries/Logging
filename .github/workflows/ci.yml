name: CI

on:
  push:
    branches: [development, master]
  pull_request:
    branches: [development, master]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  ci:
    uses: AtyaLibraries/github-workflows/.github/workflows/dotnet-package-ci.yml@v1
    with:
      solution: ./Logging.sln
      test-project: ./tests/Logging.UnitTests/Logging.UnitTests.csproj
      package-project: ./src/Logging/Logging.csproj
      allow-empty-coverage: true # scaffold has no covered code yet; set false once you add some
